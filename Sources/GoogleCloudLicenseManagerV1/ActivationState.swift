// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation

/// State of the License Key activation on the instance.
public enum ActivationState: Codable, Equatable, Sendable {
  /// The Status of the activation is unspecified
  case unspecified
  /// Activation key (MAK) requested for the instance.
  case keyRequested
  /// License activation process is running on the instance.
  case activating
  /// License activation is complete on the instance.
  case activated
  /// License Key is deactivating on the instance.
  case deactivating
  /// License Key is deactivated on the instance.
  case deactivated
  /// License Key activation failed on the instance.
  case terminated
  /// Encodes an unknown integer value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownIntValue(Int)
  /// Encodes an unknown string value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownStringValue(String)

  public init() {
    self = .unspecified
  }

  /// Returns the integer value associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown string value, this returns `nil`.
  public var intValue: Int? {
    switch self {
    case .unspecified: return 0
    case .keyRequested: return 1
    case .activating: return 2
    case .activated: return 3
    case .deactivating: return 4
    case .deactivated: return 5
    case .terminated: return 6
    case .unknownIntValue(let v): return v
    case .unknownStringValue: return nil
    }
  }

  /// Returns the string value (or name) associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
  public var stringValue: Swift.String? {
    switch self {
    case .unspecified: return "ACTIVATION_STATE_UNSPECIFIED"
    case .keyRequested: return "ACTIVATION_STATE_KEY_REQUESTED"
    case .activating: return "ACTIVATION_STATE_ACTIVATING"
    case .activated: return "ACTIVATION_STATE_ACTIVATED"
    case .deactivating: return "ACTIVATION_STATE_DEACTIVATING"
    case .deactivated: return "ACTIVATION_STATE_DEACTIVATED"
    case .terminated: return "ACTIVATION_STATE_TERMINATED"
    case .unknownIntValue: return nil
    case .unknownStringValue(let v): return v
    }
  }

  /// Initialize from a string value.
  ///
  /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ActivationState/unknownStringValue(_:)).
  public init(stringValue: Swift.String) {
    switch stringValue {
    case "ACTIVATION_STATE_UNSPECIFIED": self = .unspecified
    case "ACTIVATION_STATE_KEY_REQUESTED": self = .keyRequested
    case "ACTIVATION_STATE_ACTIVATING": self = .activating
    case "ACTIVATION_STATE_ACTIVATED": self = .activated
    case "ACTIVATION_STATE_DEACTIVATING": self = .deactivating
    case "ACTIVATION_STATE_DEACTIVATED": self = .deactivated
    case "ACTIVATION_STATE_TERMINATED": self = .terminated
    default: self = .unknownStringValue(stringValue)
    }
  }

  /// Initialize from an integer value.
  ///
  /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ActivationState/unknownIntValue(_:)).
  public init(intValue: Int) {
    switch intValue {
    case 0: self = .unspecified
    case 1: self = .keyRequested
    case 2: self = .activating
    case 3: self = .activated
    case 4: self = .deactivating
    case 5: self = .deactivated
    case 6: self = .terminated
    default: self = .unknownIntValue(intValue)
    }
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.singleValueContainer()
    if let v = try? container.decode(Int.self) {
      self.init(intValue: v)
      return
    }
    if let s = try? container.decode(String.self) {
      if let v = Int(s) {
        self.init(intValue: v)
      } else {
        self.init(stringValue: s)
      }
      return
    }
    throw DecodingError.dataCorruptedError(
      in: container, debugDescription: "Expected enum value, must be integer or string.")
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.singleValueContainer()
    switch self {
    case .unspecified: return try container.encode(0)
    case .keyRequested: return try container.encode(1)
    case .activating: return try container.encode(2)
    case .activated: return try container.encode(3)
    case .deactivating: return try container.encode(4)
    case .deactivated: return try container.encode(5)
    case .terminated: return try container.encode(6)
    case .unknownIntValue(let v): return try container.encode(v)
    case .unknownStringValue(let v): return try container.encode(v)
    }
  }
}
